# Email API Configuration Template
# Copy this to .env and fill in your own values. DO NOT commit .env to git!

# ===== RESEND CONFIGURATION (RECOMMENDED FOR PRODUCTION) =====
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here

# Email addresses
# FROM: Must use your verified domain in Resend (e.g., noreply@yourdomain.com)
# Can include display name: "Portfolio <noreply@yourdomain.com>"
EMAIL_FROM=Portfolio <noreply@yourdomain.com>

# TO: Your personal email where you want to receive contact form submissions
EMAIL_TO=malihamehnazcse@gmail.com

# ===== SMTP CONFIGURATION (FALLBACK - OPTIONAL) =====
# Only used if Resend fails or is not configured
# For Gmail: Use App Password from https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ===== CORS CONFIGURATION =====
# CORS Origins (comma-separated list or "*" for all)
# For production, set this via environment variables on Railway/Render/etc.
# Include both www and non-www versions of your domain
# For local dev, include your dev ports: http://localhost:3000,http://localhost:9002
CORS_ALLOW_ORIGIN=https://yourportfolio.com,https://www.yourportfolio.com,http://localhost:9002

# ===== OPTIONAL SETTINGS =====
# Enable dry-run mode (logs requests without sending emails - useful for testing)
EMAIL_DRY_RUN=false
