SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=malihamehnazcse@gmail.com
SMTP_PASSWORD=ibwyzfsupobkdpvv
EMAIL_FROM=malihamehnazcse@gmail.com

# CORS origins for local development. When deployed to Railway, set CORS_ALLOW_ORIGIN
# via Railway environment variables instead (this .env is baked into the Docker image).
# Support both port 3000 and 9002 for local dev.
CORS_ALLOW_ORIGIN=http://localhost:3000,http://localhost:9002,malihamehnaz.cloud

# By default this service will send contact emails to the site owner below.
# Override by setting EMAIL_TO in your environment.
EMAIL_TO=malihamehnazcse@gmail.com
